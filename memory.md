# Memory (Persistent Learnings)

This file grows over time. Add only durable lessons, corrected mistakes, and repeatable patterns.

## Learnings
- [ ] ALWAYS read the schema before analyzing (prevents hallucinated column meanings).
- [ ] For calendar access: start with read-only OAuth scopes; expand permissions only when you explicitly approve an action that writes to the calendar.

## Failure Patterns
- [ ] TBD: record tool failures with root cause + the corrected workflow.

## Preferences / Standards
- [ ] TBD: update when you tell the agent “I prefer it this way”.
- [x] Do not run commands/actions without explicit user approval.
- [x] Keep a durable record of important command workflows in memory when requested.

